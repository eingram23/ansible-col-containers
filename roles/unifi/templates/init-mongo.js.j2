db.getSiblingDB("unifi").createUser({user: "unifi", pwd: "{{ lookup('hashi_vault', 'secret=secret/ssh/eingram:ssh_password') }}", roles: [{role: "dbOwner", db: "unifi"}]});
db.getSiblingDB("unifi_stat").createUser({user: "unifi", pwd: "{{ lookup('hashi_vault', 'secret=secret/ssh/eingram:ssh_password') }}", roles: [{role: "dbOwner", db: "unifi_stat"}]});
